# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# The Cloudflare Pages upload root the deploy assembles locally.
out
# The site entry's build. The deploy lays it over the tracked hosting dir; nothing here is committed.
site-dist
coverage
release
docs-api
# Local GGUF models the desktop engine loads (dev drops them beside the project root).
models
*.local

pnpm-lock.yaml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Baseline test harness: local run outputs + machine-specific config
testing/baseline/runs/
testing/baseline/harness/profiles.json
testing/baseline/harness/profiles.json.*
testing/baseline/harness/Modelfile.*

# Graphify knowledge-graph output (trial). Un-ignore if you adopt the team-commit workflow.
graphify-out/

# Windows resource files generated by go-winres when building the launcher locally (CI regenerates them).
launcher/*.syso

# Real authored worlds used as extraction test corpora (large, and not ours to publish).
testing/authored-worlds/

# Real play-session exports used for debugging (large, contain user content).
testing/real-sessions/

# Worlds being authored/reworked locally (large exports, not published from the repo).
worlds-wip/

# Playwright browser-test output (traces/screenshots from a failed run).
test-results/
playwright-report/
.playwright/
_shots/

# Android signing material never enters the repo; the keystore lives outside it (see scripts/android-signing-wizard.sh).
*.p12
*.jks
*.keystore
