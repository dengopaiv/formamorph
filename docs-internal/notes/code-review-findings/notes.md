# Code review findings — 2026-07-22

Full-codebase review (10 parallel reviewers + jscpd + manual re-verification of every High/Med). Baseline at review time: typecheck clean, lint clean, jscpd 1.93% duplication. Items marked ✔ were re-verified against source by hand.

Check off as fixed. Grouped by severity, then theme.

## ▶ Resume here (paused 2026-07-23)

The modal-dedup line, the regen/in-turn thunk drift, the per-keystroke dirty checks, and the two download-path dups are now closed. **4 items remain** (no latent bugs left):

- **Duplication:** 1 left — `locationTree↔groupTree` projection math (nuanced; verify the projections actually match before merging).
- **Structural:** unmemoized root contexts (biggest perf debt), god components (XL), StatUpdates-by-mutable-name (needs a version/migration decision — export-shape).

Suggested next: **locationTree↔groupTree** (last small dup, but check parity first) closes the Duplication section. The **unmemoized contexts** is the biggest payoff but highest risk — do it deliberately with before/after verification. **god components** are XL (split work). **StatUpdates-by-mutable-name** needs a user version/migration decision before starting.

## High

- [x] ✔ **History-pairing corruption on failed narration** — `src/views/GameViewer.tsx:1412` (empty-response return) + catch block ~`:1767`. User msg appended at `:1269` before the request; failure paths return without dropping it. Only Stop cleans up (`:1955`). One empty/failed narration desyncs every later page and drops turns from AI context. **FIXED 2026-07-22**: extracted `discardUnpairedUserTurn()` (closure-agnostic: reverse-find for the prior assistant's choices, functional-updater drop guarded on tail `role === "user"` so a partial-streamed assistant turn is kept), called from the empty-narration exit, the catch top, and `abortGeneration`'s user branch. Gates green.
  - _Remaining sub-item (separate, lower sev):_ partial-stream-then-error leaves a `[user, partial-assistant]` pair with no `gameStates` snapshot armed → rollback/regenerate misalign for that turn. Overlaps MED abort-pipeline work.
- [x] ✔ **In-game body morphs doubled** — `src/components/game/GamePanels.tsx:144`. `Belly: bellySize + (bellySize || 0)` = 2x (also Fat/Breasts). Customization screen passes plain `x` (`src/lib/useVrmCustomization.tsx:159`). **FIXED 2026-07-22**: pass plain values. Origin investigation: `x + (x||0)` is an inert artifact from upstream FieryLionite v1.1.14 (`e608a3d`) where Belly had no slider (`bellySize` always 0), carried verbatim through the TS port; it became live once Body Weight (`Fat`) and Breasts (`Breasts`) sliders drove the same expression. Belly itself remains latent — still no Belly slider in customization. Not intentional (asymmetric `||0` guard → NaN on undefined; sibling `bodyShape` morphs pass 1×; customization preview is 1×). Gates green.
- [x] **VRMViewer loads have no cancellation** — `src/views/VRMViewer.tsx:452`. Model swap mid-load lets old GLTF/FBX callbacks run post-cleanup and start an uncleared self-rescheduling timer chain retaining the disposed scene. **FIXED 2026-07-22**: added a `cancelled` flag in the effect; guards the GLTF callback, the FBX callback, and `playNextAnimation`, and is set in cleanup. Gates green.

## Medium — gameplay & data correctness

- [x] ✔ **Trait stat-change adjustment applied per-change not per-stat** — `src/lib/statChanges.ts:152`. Stat with min+max changes lands at 40 instead of 30. **FIXED 2026-07-22**: pass 2 now applies `'starting'` deltas per-change but iterates `valueAdjustments` once per stat for the bounds adjustment. Regression test added (asserts 30, catches the old 40). Gates green.
- [x] ✔ **Corrupt localStorage crashes app at boot** — `src/lib/usePersistentState.ts:10`. `boolCodec` = bare `JSON.parse` in the `useState` initializer, used ~30× at App root; `intCodec`/`floatCodec` pass NaN through. Also bare `JSON.parse` in `SettingsContext.tsx:291,200`. **FIXED 2026-07-22**: try/catch in the initializer → `defaultValue` (covers all codecs incl. inline; write effect self-heals the bad key); numeric codecs throw on non-finite via `requireFinite`; both bare `JSON.parse` sites in SettingsContext guarded. New `usePersistentState.test.ts` (6 tests). Gates green.
- [x] ✔ **`loadWorldData` migrates then discards result** — FIXED 2026-07-23: returns `{ world, isDefault }`; GameViewer cross-world load + MainMenu details cache use the migrated world. — `src/contexts/GameDataContext.tsx:256` returns only `isDefault`; callers reuse raw un-migrated world (`GameViewer.tsx:2901`, `MainMenu.tsx:535`). Legacy IndexedDB worlds bypass the migration net. Fix: return the migrated `World`, have callers use it.
- [x] **Save-and-exit closes on failed save (×3)** — `src/views/WorldEditor.tsx:697` ✔, `src/components/modals/EntityEditorModal.tsx:151` ✔, `src/components/modals/DictionaryEditorModal.tsx:169`. `handleSave` swallows failure so `onClose()` still runs; edits lost. **FIXED 2026-07-22**: `handleSave` now returns `Promise<boolean>` in both modals (false in catch); WorldEditor's `saveWorld` already returned `ok`. All three `onSave` handlers gated: `if (await save()) onClose();`. Gates green.
- [x] **Abort/Stop pipeline gaps** — `GameViewer.tsx`: Stop during fade-reveal ignored, turn commits anyway (`:1683`); Stop during up-front location request corrupts *previous* turn's snapshot + loses choices (`:1144`); no unmount abort of in-flight AI requests (exit-to-menu mid-turn keeps LLM generating into a dead provider). **FIXED 2026-07-22**: (1) added `if (signal.aborted) return;` after `fadeReveal.drained()`; (2) new `userTurnAddedRef` (reset at turn start, set after the user message) gates `abortGeneration`'s keep-narration/snapshot branch — a pre-user-message abort now falls through to `discardUnpairedUserTurn` instead of re-snapshotting the previous turn; (3) `useEffect(() => () => abortControllerRef.current?.abort(), [])` aborts in-flight requests on unmount. Gates green. **No unit test** — abort timing lives in the 3.6k-line GameViewer monolith with no test harness; mocking the full AI pipeline for a render test is disproportionate. Logic verified by reading; the `signal.aborted`/`userTurnAddedRef` guards are the same pattern the existing aux-request checks use.
- [x] **Stat re-roll drops the turn's regen tick** — `GameViewer.tsx:805`. Re-roll applies deltas to pre-turn baseline without re-running `handleTimePassed`, never resets `heldStatChanges`; delta bar desyncs. **FIXED 2026-07-22**: extracted `applyRegenTick(hours, thresholdStats)` from `handleTimePassed` (regen + starvation, no time advance; starvation threshold reads the passed pre-turn stats). `handleRegenerateStats` now `setHeldStatChanges({})`, applies deltas un-awaited (regen stacks before stat-code, matching the live turn), then `applyRegenTick(1, baseline)` — baseline being the pre-turn stats, so starvation thresholds correctly. Removed now-dead `getStatByName`. Gates green. No unit test — logic lives in the GameViewer monolith with no harness; verified by reading, and the extracted tick reproduces the live-turn pipeline exactly (only the time-advance differs).
- [x] **Stale-response races (guard exists in sibling code)** — `SettingsContext.tsx:347` (wrong endpoint's context window persists), `src/components/community/RemoteWorldDetailsModal.tsx:45` (world A comments into world B), `src/components/menu/ManageUsersDialog.tsx:135` (stale-page double fetch), `src/components/game/TTSModal.tsx:178` (double drain loop, canceled audio into next turn). **FIXED 2026-07-22**: request-token ref pattern on all four. `detectContextWindow` + `loadComments` + `fetchUsers` each `++reqRef` at start and discard the result if superseded; ManageUsers also switched search to a `searchNonce` trigger (kills the stale-page double fetch) and URL-encodes the query (rolls in the separate raw-interpolation LOW). TTS uses a `streamGenRef` session token bumped on start/cancel: the drain loop gates its `while` and its `append` on the token (drops canceled audio), no longer resets `streamDrainingRef` in `streamStart` (kills the double loop), and re-kicks via `drainStreamRef` if a stale bail left queued work. Gates green. No unit tests — all are timing/ordering in stateful hooks without harnesses; logic verified by reading, mirrors the existing PublishModal guard.
- [x] ✔ **EditTextModal resurrects canceled edits** — FIXED 2026-07-23: `useResetOnOpen` reseeds on open; test rewritten. — `src/components/modals/EditTextModal.tsx:19` reseeds on `text` not on open. Fix: use `useResetOnOpen`.
- [x] ✔ **Settings numeric inputs persist 0 when cleared** — FIXED 2026-07-23: `numInput(raw,min)` helper on maxTokens + 6 image fields. — `src/components/modals/SettingsModal.tsx:1202` (max tokens, custom-endpoint) + image width/height/steps/CFG. `Number('')===0` → localStorage. Fix: map `''`→null/clamp like `contextWindow`/`activeCharacterLimit` already do.
- [x] **Hang-forever I/O** — ComfyUI `pollHistory` loops forever on server-side run failure (`src/lib/imageGen/comfyui.ts:241`, InvokeAI handles it); community download no abort/timeout (`src/lib/fetchCatalogContent.ts:14`); desktop update no error path out of "downloading" (`src/hooks/useUpdateChecker.ts:72`). **FIXED 2026-07-22**: (1) `pollHistory` inspects the history entry's `status` — a terminal-but-imageless run throws `comfyHistoryError` (with the node's exception if present) instead of polling forever; also a `MAX_POLL_ERRORS=5` cap on consecutive non-ok fetches (mirrors InvokeAI). (2) `fetchCatalogContent` gained an optional `signal` + a 30s stall-timeout (reset per chunk) via an internal AbortController — a silent server no longer hangs `reader.read()`. +2 tests (stall abort, caller cancel). (3) `useUpdateChecker.download` now `.catch`es the bridge promise → dispatches `ERROR` (phase leaves 'downloading'). Gates green. ComfyUI path not unit-tested (provider needs a websocket-watcher harness); status-check is defensively optional so it degrades to old behavior if the shape differs.
- [x] ✔ **Semver prerelease compares lexicographically** — `src/lib/updates/semver.ts:32`. `beta.10 < beta.9`; prerelease users never offered `.10`. **FIXED 2026-07-22**: added `comparePrerelease` (per-identifier, numeric-numerically, numeric<alphanumeric, more-fields-wins). +2 tests (numeric ordering, identifier precedence).
- [x] ✔ **Preset share codes pass `samplers` through unvalidated** — `src/lib/promptPresetShare.ts:125`. String temperature reaches request body → 400s. **FIXED 2026-07-22**: `sanitizeSamplers` keeps only `{custom:boolean, value:finite number}` per known sampler key, drops the rest (mirrors the reasoning/verbatim sanitizers). +1 test.
- [x] ✔ **Character-card export fails on well-compressed portraits** — `src/lib/entityFile.ts:104`. never-grow guard returns original non-WebP, then export throws. **FIXED 2026-07-22**: added `allowGrow` param to `encodeImageDataUrl` (default false — existing callers unchanged) that skips the never-grow guard, exposed as `optimizeToWebpDataUrl`; `exportEntityCard` uses it so the card always gets WebP. No unit test (canvas/createImageBitmap encode path isn't available in jsdom).
- [x] **Choices scene-roster override misses XML entity variants** — `src/views/GameViewer.tsx:1496` (dup `:782`). Override spreads only 4 of 6 entity tokens; `<ENTITIES|xml>` gets full roster, defeating presence filter. **FIXED 2026-07-22**: hoisted both blocks into a shared `sceneEntityOverride(sceneLoc, sceneEntities)` useCallback that emits all 6 unscoped `<ENTITIES>` variants (full/summary × simple/markdown/xml), matching addScoped's `""`-scope set. Both call sites (main-turn `runChoices` + `handleRegenerateChoices`) spread it. Not prompt-text (mechanical context assembly), default prompts unaffected → no probe needed. Gates green.

## Medium — Electron shell (defensive security)

- [x] ✔ **`app://` containment is raw `startsWith(DIST)`** — FIXED 2026-07-23: `path.relative(DIST, filePath)` must not start with `..` or be absolute. — `electron/main.cjs:233`. Sibling dir prefixed `dist` escapes (reproduced). Fix: `path.relative(DIST, filePath)` must not start with `..`, or append `path.sep`.
- [x] ✔ **No `will-navigate` guard** — FIXED 2026-07-23: `will-navigate` handler pins the window to `app://local` (or the dev URL); external http(s) go to the OS browser. — `electron/main.cjs:80` only has `setWindowOpenHandler`. In-window nav to remote origin keeps the preload bridge (`net-fetch` → localhost). Fix: add a navigation guard pinned to `app://local`.
- [x] ✔ **Stream downloaders crash main process** — FIXED 2026-07-23: persistent `on(error)` + drain-race in modelDownload.cjs and winUpdate.cjs; .part preserved on model download. — `electron/modelDownload.cjs:75`, `electron/winUpdate.cjs:38`. WriteStream has no `'error'` listener; disk-full mid-download throws unhandled / hangs drain. Fix: attach `'error'` handler, preserve `.part`.
- [x] ✔ **Update integrity gaps** — FIXED 2026-07-23: winUpdate fails closed on missing checksum; Go launcher re-verifies staged zip sha512 before unzip (sha512File helper). RELEASE NOTE: every release MUST ship its .sha512 sidecar or auto-update refuses. — checksum silently skipped when `.sha512` sidecar missing/errors (`electron/winUpdate.cjs:55`); Go launcher parses `Sha512` but never re-verifies staged zip before swap (`launcher/main.go:27`, TOCTOU). Fix: fail closed on missing hash; re-verify in launcher before extract.

## Duplication (fix-one-miss-other)

- [x] **Regen handlers vs in-turn thunks** — DONE 2026-07-23: extracted `requestChoices(ctx, sceneEntityTokens, action, narration, signal, quiet?)` and `requestStats(ctx, action, narration, signal, quiet?)` (prompt assembly + language clause + `makeAIRequest`) as component-body helpers. The live-turn `runChoices`/`runStats` thunks and the standalone `handleRegenerateChoices`/`handleRegenerateStats` re-rolls now both call them, so the request shape can't drift. Adversarially verified all 4 call sites produce byte-identical prompts + `makeAIRequest` args (regen's defaulted `quiet=false` matches the old trailing defaults); prompt templates untouched → no probe needed. No unit test (GameViewer monolith has no harness; verified by reading, per the abort-pipeline precedent). Gates green.
- [x] **AddDictionaryModal ↔ AddEntityModal** — DONE 2026-07-23: extracted generic `AddFromLibraryModal<TMeta,TFull,TItem>` (list/select/load/copy/close); both are now thin wrappers passing `loadMetadata`/`loadRecord`/`copy`/`renderRow`. Existing AddDictionaryModal test passes unchanged through the wrapper; +3-test AddEntityModal suite (was untested). Gates green.
- [x] **EntityEditorModal ↔ DictionaryEditorModal** — DONE 2026-07-23: extracted the shared chrome into `EditorModalShell` (dialog surface, title + 2-tab header, Download/Publish/Save footer, unsaved-changes-on-close handshake incl. its own `showUnsaved` state + `attemptClose`). Each editor keeps its own data model/body/save; the shell takes tab labels, `loading`, `hasUnsavedChanges`, and the save/download/publish handlers. The save-fail bug they shared was already fixed (Medium tier), so this was pure dedup. +5-test `EditorModalShell` suite (loading gate, footer states, Publish conditional, save-and-exit-only-on-success via the Escape→unsaved-prompt flow — both were previously untested). Verified live via dev-router (both editor modals). Gates green.
- [x] **Half-finished `useEditingDraft` migration** — DONE 2026-07-23: LocationManager, GroupManager, EntityGroupManager, StatUpdatesManager, PlaceholderManager all migrated to the hook (setField/apply). — 5 managers still hand-roll draft mirror: `LocationManager`, `GroupManager`, `EntityGroupManager`, `StatUpdatesManager`, `PlaceholderManager`.
- [x] **LocationManager ↔ EntityFields** — DONE 2026-07-23: extracted the image-upload → embedded-prompt confirm → Image Tags → generate stack into `ImageTagsField` (owns its own `pendingPrompt` state). Both editors now render `<ImageTagsField ... />` with kind/cap/label/field props. The "latent bug" was that `onConfirm` never cleared `pendingPrompt` itself and relied incidentally on Radix's auto-close firing `onOpenChange(false)` — not user-observable today (Radix does close), but fragile; the extracted `onConfirm` now clears explicitly. +2-test suite (confirm→apply+clear, cancel→no-apply). Verified live via dev-router (entityEditor modal): field order + Image Tags + Generate button + confirm dialog all render. Gates green.
- [x] **KeywordChips re-implements TokenAutocomplete** — PARTIAL 2026-07-23: fixed the real drag bug (`horizontalListSortingStrategy` → `rectSortingStrategy` on the flex-wrap container). NOT changing dedup to case-insensitive: dictionary keyword matching honors a per-entry `caseSensitive` flag, so distinct-case keywords can be meaningful — the dedup "drift" is correct divergence, not a bug. Full extraction (replace KeywordChips with TokenAutocomplete) left as optional. with drift — ✔ wrong `horizontalListSortingStrategy` on wrapping container (`src/components/KeywordChips.tsx:76`; sibling uses `rectSortingStrategy`) + case-sensitive dedup vs sibling case-insensitive.
- [x] Smaller (partial): DONE 2026-07-23 — consolidated the two `clamp`s (narrationRevealConfig re-exports utils'), `escapeRegExp` recopy (dictionaryUtils imports utils), `fmtTime`→`formatMMSS` + `useVolumeMute` hook (AudioPlayer/TtsPlaybackBar), autoScroll config ×5 → `CONTAINED_AUTO_SCROLL`.
  - **Download-loop dup (`.cjs`)** — DONE 2026-07-23: extracted `pumpResponseToFile(body, out, received, onChunk)` into `electron/streamDownload.cjs` (the backpressure + `writeErr`/drain loop + final `out.end`). `modelDownload.cjs` and `winUpdate.cjs` both call it; each keeps its own open/seed/rename/verify and progress-payload shape. Byte-equivalent (the 'error' listener stays attached so callers' cleanup is still covered). +5-test `streamDownload.test.mjs` (progress, resume-seed, backpressure/drain, both error paths). Gates green.
  - **`useDownloadCoordinator` clone paths** — DONE 2026-07-23: the ~55-line `handleDownloadWorld` (add) and `overwriteWorld` (replace) now share `downloadAndStore(world, localId, commit, messages)`; the two differ only in the id, the add-vs-replace `commit` callback, and the success/error wording (passed explicitly so console/toast strings are unchanged). Field-by-field parity verified. +2-test suite (add appends fresh-id record w/ isLoading:false; overwrite replaces in place, no duplicate). Gates green.
  - **▶ PARKED / resume here (2026-07-23):** `locationTree`↔`groupTree` projection math is the ONLY open Duplication item. `src/lib/locationTree.ts` and `src/lib/groupTree.ts` both project a flat list into a nested tree for the DnD managers. Before extracting: diff the two and confirm they genuinely share semantics (ordering, orphan handling, depth rules) — they may have intentionally diverged, in which case leave them (like the KeywordChips dedup verdict). Low risk, low priority. Not a release blocker.

## Structural (larger refactors)

- [ ] **Unmemoized root contexts** — GameData/Gameplay/Settings rebuild 60–120-field `value` every render; one settings keystroke rerenders all consumers. Biggest structural debt.
- [ ] **God components** — GameViewer (3.6k, hand-maintained turn invariants), MainMenu (1.9k, ~40 useState), SettingsModal (1.8k, 6 domains). (The 2 MainMenu `Date.now()` ID violations are already fixed.)
- [x] **Expensive dirty checks** — DONE 2026-07-23: new `memoStringify(value, weakMapCache)` util — a `JSON.stringify` that caches each object/array's serialization by identity, so an edit only re-serializes the touched record (unchanged base64 images hit the cache). Byte-identical to `JSON.stringify` for plain data (verified by a 22-case parity test incl. NaN/Infinity, undefined/function keys, array holes, special chars), so it drops in against the existing `JSON.stringify` baselines with no semantic change. Wired into `isWorldDirty` (`GameDataContext`) + both editor-modal `hasUnsavedChanges` (each with its own long-lived `useRef` WeakMap). Safe under the app's immutable-update convention (in-place mutation without a new reference wouldn't re-render anyway, so the cache can't be more stale than the render). Live-verified: Save flips disabled→enabled on first keystroke. Gates green.
- [x] **Per-save IndexedDB connections never closed** — FIXED 2026-07-23: `initDB` now caches the connection promise (`.catch` clears it so a failed open retries), mirroring `worldCatalog`/`thumbnailCache`. Autosave reuses one handle instead of leaking one per op. +2 tests (same-connection across calls; retry after failed open). Gates green. — `src/components/modals/dbUtils.ts:16`. Autosave opens 2+/turn; will block a future `DB_VERSION` bump.
- [ ] **StatUpdates reference stats by mutable name** — renaming a stat silently orphans StatUpdates (`StatUpdatesManager.tsx:26`). Data-model design decision.

## Low — quick hygiene

- [x] ✔ **Two `Date.now()` world IDs** — `src/views/MainMenu.tsx:824,868`. **FIXED 2026-07-22**: both now `randomUUID()` (prefix kept). IDs aren't exported (world export strips `id`), so no migration. Gates green.
- [x] ✔ **Named world exports without `.json`** — `src/views/WorldEditor.tsx:134`. **FIXED 2026-07-22**: `` `${worldOverview.name || 'rpg_world'}.json` ``. Gates green.
- [x] **Stale/false comments** — **FIXED 2026-07-22**: MainMenu:976 "only Worlds is wired up" dropped (all 4 tabs work); GamePrompts.ts:76 `<GAME TEXT>` → `<NARRATION>` (confirmed the token doesn't exist anywhere; real one is `<NARRATION>`). The 5s VRM delay is NOT a comment fix — moved to its own item below.
- [x] **Unexplained 5s VRM-viewer mount delay** — `src/components/game/GamePanels.tsx:126`. **FIXED 2026-07-22**. Root cause found: `usePlayerModelUrl` resolves a library model's blob URL async (undefined→URL), and VRMViewer is keyed on `playerModelUrl ?? 'default'`, so mounting immediately would transiently show the bundled default (reports default capabilities, flashes) before remounting to the real model. The 5s timer was a crude wait for that async resolution — CharacterCustomization already handled it properly with a `resolvingModel` heuristic. Fix: added a real `resolving` flag to `usePlayerModelUrl` (true only while a library blob is fetching, clears on settle incl. failure → deleted model no longer hangs). GamePanels gates the viewer on `resolving` (removed the timer + `showVRMViewer` state); CharacterCustomization now uses the hook's flag instead of its `!resolvedModelUrl` heuristic (which couldn't tell fetching from failed-fell-back). Default/world models mount instantly; library models mount after just their load time. +4 renderHook tests (resolving lifecycle, deleted-no-hang, default/world sync). Gates green.
- [x] **DictionaryTree downward-drag off-by-one** — `src/lib/dictionaryTree.ts` `moveEntryInBooks`. **CONFIRMED + FIXED 2026-07-22**. Verification: each zone is its own `SortableContext` with `verticalListSortingStrategy` (arrayMove preview) + `closestCorners`; the drop committed insert-before-`over` on post-strip indices → for a same-zone downward drag, preview `[b,c,a]` vs commit `[b,a,c]` (off by one); upward matched. (Live drag couldn't be driven — the preview pane wasn't compositing frames and the collapsible entry handles don't select cleanly from the DOM — so confirmation is at the dnd-kit library-semantics level, which is definitive.) Fix: detect a same-book same-zone downward reorder from the pre-move positions and insert *after* `over` (arrayMove parity); upward/cross-zone/cross-book keep insert-before. +4 tests. Gates green.
  - **Follow-up (fixed 2026-07-23):** initial fix compared `entry.position` with strict `===`, but an untouched entry has `position: undefined` (renders in the default 'after'/Foreground zone) while a previously-moved entry carries an explicit `'after'`. `undefined === 'after'` is false, so a same-zone downward drag of an untouched entry was misread as cross-zone → insert-before → landed one slot too high. This surfaced as "first drag wrong, subsequent correct" because `moveEntryInBooks` stamps an explicit `position` on every moved entry. Diagnosed via temporary drag-end logging in the live app (dnd-kit `over` was correct; the committed order revealed the position mismatch). Fix: default both sides with `zone(e) = e.position ?? 'after'` before comparing. Added the mixed-position regression test.
  - **Second follow-up (fixed 2026-07-23):** a distinct, PRE-EXISTING bug surfaced during testing — dropping an entry on itself (pick-up with no move; dnd-kit fires drag-end with `over === active`) stripped the entry then `findIndex(over)` returned -1 (over IS the stripped entry), so the `-1 → entries.length` fallback appended it to the zone's end. Any in-place drag banished the item to the bottom. Fix: early-return `books` unchanged when `overEntryId === entryId` (mirrors `arrayMove(i, i)`). Diagnosed via live drag-end logging (the "clear repro" was the user noticing a no-move drag always misbehaves). +1 test.

Additional lower-severity items (index-key reuse in message list / manager rows, ModelViewer disposal gaps, `announce` timers not cleared, aiResponse fallback regex, effort-probe empty-token header, etc.) are in the reviewer transcripts if needed — not enumerated here to keep the actionable list focused.
